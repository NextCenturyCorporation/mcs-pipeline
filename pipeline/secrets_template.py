#
#   Secrets file.    Copy the secrets_template.py file to secrets.py and fill it in.
#   You should not add secrets.py to the github project;  secrets.py is in .gitignore.
#

Secrets = {
    'PEM_FILE': "",
    'USERNAME': 'ubuntu',
    # 'USERNAME': 'ec2-user'
}
