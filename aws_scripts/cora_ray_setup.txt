#This file is not meant to executed.  It is intended to be a helper to know what commands should be run.

# Startup
ray up -y autoscaler/ray_cora_aws.yaml

# Copying over necessary files
ray rsync_up -v autoscaler/ray_cora_aws.yaml configs/ '~/configs/'
ray rsync_up -v autoscaler/ray_cora_aws.yaml /home/clark/work/mcs/eval3.5/data/eval/ /home/ubuntu/scenes
ray rsync_up -v autoscaler/ray_cora_aws.yaml scenes_single_scene.txt '~'

# Starting the job
ray submit autoscaler/ray_cora_aws.yaml pipeline_ray.py configs/cora_aws.ini configs/mcs_config_cora_level2.ini

# shutdown
ray down -y autoscaler/ray_cora_aws.yaml
