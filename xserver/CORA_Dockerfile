#
# Dockerfile that adds the X server to the CORA Submission
#

FROM probcomp/pram-gpu:0.3.8

RUN apt install -y emacs tree silversearcher-ag

RUN apt install -y xserver-xorg
RUN apt install -y pciutils


ADD . /x_server

RUN NVIDIA_VERSION=450.80.02 /x_server/install_nvidia.sh