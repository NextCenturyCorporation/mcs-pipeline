[MCS]
debug = false
metadata = ${metadata}
evaluation = ${evaluation_bool}

s3_bucket=${s3_bucket}
s3_folder=${s3_folder}
s3_movies_folder=${s3_movies_folder}

video_enabled=${video_enabled}
history_enabled=${history_enabled}
save_debug_images=${save_debug_images}

team=${team}
evaluation_name=${eval_name}
logs_to_s3=true

${mcs_config_extra}
